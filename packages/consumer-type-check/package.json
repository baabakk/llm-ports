{
  "name": "@llm-ports/consumer-type-check",
  "version": "0.1.0-alpha.24",
  "private": true,
  "description": "Strict-mode TypeScript smoke test against the public surface of every published @llm-ports/* package. Compiles in CI; catches type-level breaking changes the per-package tests miss because they only see internals.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@llm-ports/core": "workspace:*",
    "@llm-ports/capabilities": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
